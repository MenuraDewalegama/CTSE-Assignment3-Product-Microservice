apiVersion: v1
kind: Service
metadata:
  name: product-api-service
spec:
  type: NodePort
  selector:
    app: product-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30100